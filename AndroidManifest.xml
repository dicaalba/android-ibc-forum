<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	android:versionCode="1"
	android:versionName="1.0" package="de.mtbnews.android">

	<application android:icon="@drawable/ibc_icon" android:label="@string/app_name" android:theme="@style/IBC">
		<activity android:name=".IBCActivity" android:label="@string/app_name">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

		<activity android:name="Configuration" android:label="@string/preferences"></activity>

		<activity android:name="ForumActivity" android:label="@string/forum"></activity>
		<activity android:name="NewsActivity" android:label="@string/news"></activity>
		<activity android:name="PhotoActivity" android:label="@string/photos"></activity>

		<service android:name=".service.UploadIntentService"></service>
	
</application>

	<uses-permission android:name="android.permission.INTERNET" />

	<!-- Android 1.6 -->
	<uses-sdk android:minSdkVersion="4"></uses-sdk>

</manifest> 